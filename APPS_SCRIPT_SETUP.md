# 보안 Apps Script 연결 방법

## 1. 새 Code.gs 적용

1. 스프레드시트를 `202620626@dj.hs.kr` 계정으로 엽니다.
2. **확장 프로그램 → Apps Script**에서 기존 `Code.gs`를 이 폴더의 새 파일로 교체하고 저장합니다.
3. `verifyConnection`을 한 번 실행해 권한을 승인합니다.

## 2. 비밀번호를 코드 밖에 설정

Apps Script 왼쪽 **프로젝트 설정 → 스크립트 속성 → 스크립트 속성 추가**에서 다음 두 항목을 만듭니다.

| 속성 | 용도 |
| --- | --- |
| `VIEWER_PASSWORD` | 학생 현황을 볼 때 사용하는 학교 내부 접속 비밀번호 |
| `ADMIN_PASSWORD` | 벌점 추가·정정·최근 기록 삭제에 사용하는 관리자 비밀번호 |

두 비밀번호는 서로 다르게 정하고, 12자 이상의 추측하기 어려운 값으로 설정하세요. 비밀번호를 `script.js`, `config.js`, GitHub 저장소 또는 문서에 적지 마세요.

## 3. 웹 앱을 새 버전으로 배포

1. **배포 → 배포 관리**에서 기존 웹 앱의 편집 버튼을 누릅니다.
2. 버전을 **새 버전**으로 선택합니다.
3. 실행 사용자는 `202620626@dj.hs.kr`, 액세스 권한은 정적 사이트가 호출할 수 있는 범위로 둡니다.
4. 배포한 뒤 `/exec` 주소를 직접 열어 `v44-secure-pwa`와 “보호된 API가 정상 작동 중입니다”가 표시되는지 확인합니다.
5. `/exec` 주소가 바뀌었다면 `config.js`의 `APPS_SCRIPT_URL`도 갱신합니다.

## 4. GitHub Pages 반영

이 폴더의 파일을 저장소 루트에 업로드하고 GitHub Pages 배포가 끝날 때까지 기다립니다. 기존 서비스 워커가 남아 있을 수 있으므로 첫 반영 후 브라우저를 완전히 닫았다 다시 열거나 새로고침하세요.

## 5. 휴대폰 설치

- Android Chrome: 메뉴 → **홈 화면에 추가** 또는 **앱 설치**
- iPhone Safari: 공유 버튼 → **홈 화면에 추가**

앱은 화면과 정적 파일만 오프라인으로 보관합니다. 학생 정보와 비밀번호는 서비스 워커 캐시에 저장하지 않습니다.

## 출시 전 확인

- `privacy.html`의 학교 담당자 이름과 공식 문의 연락처를 입력합니다.
- 접속 비밀번호를 학생·교직원 외부에 공유하지 않습니다.
- 기존 공개 버전은 새 보안 버전 배포 직후 교체합니다.
- 이전에 공개했던 `daejin1234`는 다시 사용하지 않습니다.
